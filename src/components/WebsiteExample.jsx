function WebsiteExample({ name, description }) {
    return (
        <div>
            website example
            <h3>{name}</h3>
            <p>{description}</p>
        </div>
    )
}

export default WebsiteExample
